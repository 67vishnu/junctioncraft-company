# Here are your Instructions
for every new change or new repo
change package.js home page to domain before that connect domain 
add deploy.yml file you cann do it from repo setting> page> under giyaction u will get 
